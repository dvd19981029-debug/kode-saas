# Reglas de Comportamiento del Agente

- **Subir a GitHub al Instante:** Cada vez que realices un cambio en el código, pruebes una funcionalidad o finalices una tarea, debes ejecutar `git add .`, realizar un `git commit` descriptivo y un `git push` a GitHub de forma inmediata, sin esperar a que el usuario lo solicite.
